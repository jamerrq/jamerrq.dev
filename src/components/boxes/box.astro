---
interface Props {
  ncols?: number
  nrows?: number
  animated?: boolean
  delay?: number
  title: string
  otherStyles?: string
}

const { props }: { props: Props } = Astro
---

<article
  data-animated={props.animated}
  class=`card bg-slate-300/90 dark:bg-slate-900/95 rounded-[10px] flex items-center justify-center font-bold dark:text-slate-300 font-primary w-full h-full flex-col text-base py-3 px-2 relative border-2 border-transparent hover:border-emerald-500 transition-colors duration-200 data-[animated]:animate-blurred-fade-in data-[animated]:animate-delay-${(props.delay ?? 0) * 100} ${props?.otherStyles} text-emerald-950`
>
  <slot />
</article>
